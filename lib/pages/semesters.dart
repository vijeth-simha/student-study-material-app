import 'package:flutter/material.dart';

class SemesterPage extends StatelessWidget {
  const SemesterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Semester page done");
  }
}
